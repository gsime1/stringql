name: Testing
on: push

jobs:
  test_stringql:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run unit tests
      run: |
        pip3 install --user testing.posgresql
        python3 stringql/pg_engine_test.py test